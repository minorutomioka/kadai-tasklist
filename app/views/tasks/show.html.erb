<h1>id: <%= @task.id %>のタスク</h1>
<div class="mb-5">
  <table class="table table-boarderd col-md-6">
  <tr>
    <th>status</th>
    <td><%= @task.status %></td>
  </tr>
  <tr>
    <th>content</th>
    <td><%= @task.content %></td>
  </tr>
  </table>
</div>
<%= link_to 'このタスクを編集する', edit_task_path(@task) , class: 'btn btn-primary' %>
<%= link_to 'このタスクを削除する', @task, method: :delete, class: 'btn btn-warning' , data: { confirm: '本当に削除してよろしいですか？'}%>
